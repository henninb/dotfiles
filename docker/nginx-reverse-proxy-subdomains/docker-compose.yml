version: "3.7"
services:
  nginx-reverse-proxy:
    build: .
    container_name: nginx-reverse-proxy
    # extra_hosts:
      # - "pihole.proxy:127.0.0.1"
      # - "pfsense.proxy:127.0.0.1"
      # - "proxmox.proxy:127.0.0.1"
      # - "ddwrt.proxy:127.0.0.1"
      # - "hornsup.proxy:127.0.0.1"
    ports:
      - "443:443"
    expose:
      - "443"
