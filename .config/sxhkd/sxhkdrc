alt + shift + Return
    urxvt
alt + Return
#    termite
    alacritty
alt + shift + i
    firefox -private-window
alt + i
    firefox
alt + shift + p
    dmenu_run -nb orange -nf '#444' -sb yellow -sf black -fn Monospace-9:normal
alt + shift + Escape
    bspwm_exit "bspc quit"
alt + shift + BackSpace
    xdo close
#alt + shift + Delete
#    xeyes
#    bspc  window -c
#    bspc  window -k
#super + shift + Escape
#    bspwm_exit "bspc quit"
# alt + shift + q
#    prompt "Are you sure you would like to quit" "bspc quit"
#    bspc quit 1 && pkill bar && pkill dzen2
XF86AudioRaiseVolume
    amixer -q -D pulse sset Master 2%+
XF86AudioLowerVolume
    amixer -q -D pulse sset Master 2%-
XF86AudioMute
    amixer -D pulse sset Master toggle
#XF86AudioPlay
#   $HOME/.config/bspwm/panel/notify_mpd -t
#XF86AudioNext
#   $HOME/.config/bspwm/panel/notify_mpd -n
#XF86AudioPrev
#   $HOME/.config/bspwm/panel/notify_mpd -p
# make sxhkd reload its configuration files:
# alt + shift + Escape
#    pkill -USR1 -x sxhkd
alt + {_,shift + }{1-9,0}
      bspc {desktop -f,node -d} '^{1-9,10}' --follow
