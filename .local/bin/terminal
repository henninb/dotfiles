#!/usr/bin/env sh

# if alacritty -e tmux new-session -A -s 'Default'; then
if alacritty; then
  exit 0
fi

if st; then
  exit 0
fi

if urxvt; then
  exit 0
fi

if xterm -fa 'monofur for Powerline' -fs 12; then
  exit 0
fi

exit 0

# vim: set ft=sh:
