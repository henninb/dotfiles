#!/bin/sh

# MAX_ITERATIONS=30
# SLEEP_TIME=0.5
# for ((i=0; i<$MAX_ITERATIONS; i++))
# do
#     if pgrep -x "yourapp" >/dev/null
#     then
#         echo "Your app is running!"
#         xdotool search --class "YourApp" windowactivate %@
#         break
#     else
#         sleep $SLEEP_TIME
#     fi
# done

if command -v streamdeck; then
  nohup streamdeck
  # streamdeck & sleep 2 && xdotool search --class "Streamdeck UI" windowminimize
else
  notify-send "streamdeck is NOT installed"
fi

exit 0

# vim: set ft=sh:
