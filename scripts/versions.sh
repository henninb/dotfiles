#!/bin/sh

javac --version
node --version
nvim --version | head -1
xmonad --version
stack --version
alacritty --version
keepassxc --version
dzen2 -v
netlify --version
dunst --version
kitty --version
nix-env --version
starship --version
arduino-cli version
clang --version

exit 0
