set postmaster "henninb@localhost"
set no bouncemail
set properties ""

# poll pop.gmail.com
#   with no dns
#   with proto POP3
#   user "henninb08"
#   pass ""
#   # is henninb here
#   # smtpaddress localhost
#   mda "/usr/sbin/sendmail  -i  -f %F -- %T"
# options
#   warnings 3600
#   antispam 571 550 501 554
#   no keep
#   ssl
#   fetchall

# mda "/usr/bin/procmail -d %T":   user "henninb" password "p1nk" mda "/usr/bin/procmail -d %T"

# poll imap.gmail.com
poll imap.gmail.com
   with protocol IMAP
# poll imap.gmail.com protocol IMAP
   user "henninb08@gmail.com"
   is henninb here
   password ""
   # folder "INBOX"
   fetchall
   no keep
   ssl
   # keep
   # mda '/usr/bin/procmail -f %F -d %T'
poll imap.gmail.com
   with protocol IMAP
   user "henninb01@gmail.com"
   is henninb here
   password ""
   # folder "INBOX"
   fetchall
   #fetchlimit 1
   no keep
   ssl

poll imap.gmail.com
   with protocol IMAP
   user "brian.henning@gmail.com"
   is henninb here
   password ""
   # folder "INBOX"
   fetchall
   #fetchlimit 1
   no keep
   ssl

poll imap.gmail.com
   with protocol IMAP
   user "brianxhenning@gmail.com"
   is henninb here
   password ""
   # folder "INBOX"
   fetchall
   #fetchlimit 1
   no keep
   ssl
